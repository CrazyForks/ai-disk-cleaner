# 数据模型目录规范

- 每个数据模型必须放在 `backend/data/models` 下独立的文件夹中，文件夹名应与数据模型对应。
- 禁止把不同数据模型的定义或持久化逻辑写在同一个文件或同一个模型文件夹中。
- 新增数据模型时，必须创建对应的独立文件夹，并将该模型的结构体、GORM 钩子及持久化方法放入该文件夹。
- 模型之间的共享组合与数据库初始化逻辑应放在 `backend/data` 中，不得通过合并模型文件来复用。
